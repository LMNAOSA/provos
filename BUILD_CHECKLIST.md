# Build checklist

1. Confirm `public/images/Matrixtwin_opal.glb` is committed to GitHub.
2. Run `npm install`.
3. Run `npm run build`.
4. Deploy to Vercel.
5. Confirm `/images/Matrixtwin_opal.glb` returns HTTP 200.
6. Test Case 001 through all six stages on desktop and mobile.
